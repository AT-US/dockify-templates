apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: nginx
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`HOSTNAME_PLACEHOLDER`)
      kind: Rule
      services:
        - name: nginx
          port: 80
  tls:
    secretName: WILDCARD_CERT_SECRET
