apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: nginx
spec:
  entryPoints:
    - websecure
  routes:
    - match: HostSNI(`*`)
      services:
        - name: nginx
          port: 80
  tls:
    passthrough: false
    secretName: WILDCARD_CERT_SECRET
