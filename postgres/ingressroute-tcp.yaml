apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: postgres
spec:
  entryPoints:
    - postgresql
  routes:
    - match: HostSNI(`postgres.dockify.cloud`)
      services:
        - name: postgres-rw
          port: 5432
  tls:
    passthrough: true
