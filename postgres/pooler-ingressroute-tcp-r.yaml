apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: postgres-pooler-r
spec:
  entryPoints:
    - postgres
  routes:
    - match: HostSNI(`pooler-r.namespace.w1.dockify.cloud`)
      services:
        - name: postgres-pooler-r
          port: 5432
  tls:
    passthrough: true
