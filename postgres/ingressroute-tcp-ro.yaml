apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: postgres-ro
spec:
  entryPoints:
    - postgres
  routes:
    - match: HostSNI(`namespace-ro.w1.dockify.cloud`)
      services:
        - name: postgres-ro
          port: 5432
  tls:
    passthrough: true
