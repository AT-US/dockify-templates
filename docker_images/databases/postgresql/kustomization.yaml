apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - secret.yaml
  - pvc.yaml
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  - ingressroute-tcp.yaml
  - networkpolicy.yaml
  # Optional components (uncomment if needed):
  # - pgbouncer-deployment.yaml  # Connection pooling (requires config fixes)
  # - pgbouncer-service.yaml     # PgBouncer service
  # - backup-simple.yaml         # Backup CronJob

namespace: default

labels:
  - pairs:
      app: postgresql
      managed-by: dockify
