apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - secret.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - ingressroute-tcp.yaml
  - networkpolicy.yaml

namespace: default

labels:
  - pairs:
      app: postgresql
      managed-by: dockify
